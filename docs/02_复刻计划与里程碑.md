# MyNature 复刻计划与里程碑

> 目标：在 `g:\Na\MyNature` 从零复刻 IL3DN 的 **Demo 场景 + 冬季松林场景**（高保真）。
> 源项目调研：见 `01_源项目调研报告.md`。
> 教学主线（逐集视频）：见 `03_教程大纲_逐集产出.md`。
> 视频制作规范：见 `04_视频制作规范_AutoVideo.md`。
>
> ⚠️ 本文档为 2026-07-22 全面重写版，旧版中"Unity CLI 支柱 / 环境待搭建 / 仅 Demo 场景"等假设已全部更新。

---

## 1. 关键技术决策（已与用户确认，2026-07-22）

| 维度 | 决策 | 说明 |
|---|---|---|
| Unity 版本 | **Unity 6000.5.4f1**（已安装，项目已存在） | URP **17.5.0**，模板项目已建好，无需再创建 |
| 复刻范围 | **Demo 场景 + 冬季松林场景** | 冬季场景源文件**存在**（31 MB YAML，8087 实例，2026-07-22 复核更正），与 Demo 同级按场景解析精确还原（见阶段 6/8） |
| 着色器 | **手写 HLSL**（逻辑照搬，API 适配 URP 17.5） | 不用 Shader Graph |
| 贴图 | **程序化生成**（Python/PIL） | 色卡、噪声、渐变、法线全代码生成，输出 PNG |
| 3D 模型 | **Blender MCP 生成**（已安装就绪） | `tools/blender/` 已有 addon 与启动脚本，端口 9876 |
| 音频 | **复用源项目**（42 wav + mixer） | 唯一允许复制的源资源；`Jumnp` 拼写错误文件名原样保留 |
| 输入系统 | **保留旧 Input Manager** | `Active Input Handling = Both`，忠实移植 FPSController，不重写 |
| 自动化装配 | **Editor 脚本为核心 + 多种触发器**（详见 §4A 自动化架构） | 业务逻辑写在 Editor 脚本里（与触发方式解耦）；触发保底用 `-batchmode -executeMethod`（零依赖）；Unity CLI / Unity MCP 作可选增强 |

### 工具链总览（四大支柱）

```
┌──────────────────────────────────────────────────────────────┐
│  1. Unity 6000.5.4f1 + URP 17.5     ← 渲染与运行时（已就绪）  │
│  2. 手写 HLSL (.shader/.hlsl)       ← 视觉风格               │
│  3. Blender MCP (bpy, 端口 9876)    ← 3D 建模（已就绪）       │
│  4. Python/PIL                      ← 程序化贴图              │
│  + 经典 Editor 脚本 (MenuItem)      ← 批量 Asset 装配         │
└──────────────────────────────────────────────────────────────┘
```

每集教程通常围绕其中 1-2 个支柱展开，保证单集知识点聚焦。

---

## 2. 当前状态盘点（2026-07-22 实测）

| 项 | 状态 | 备注 |
|---|---|---|
| Unity 项目 | ✅ 已存在 | `g:\Na\MyNature`，URP 模板，含 SampleScene、PC/Mobile RP Asset |
| Unity 版本 | ✅ 6000.5.4f1 | URP 17.5.0 |
| Input System 包 | ✅ 已装 1.19.0 | 但 FPSController 用旧 Input Manager，需设 `Both` |
| Blender + MCP | ✅ 已就绪 | `tools/blender/blender_mcp_addon.py` + `start_with_mcp.py` |
| Python | 待验证 | 需 Pillow + numpy |
| 音频复制 | ⬜ 未做 | 源 `Assets/IL3DN/Audio/`（42 wav + mixer）→ `MyNature/Assets/Audio/`；注意 `Jumnp` 拼写错误文件名原样保留 |
| 目录骨架 | ⬜ 未做 | `Assets/MyNature/` 下各子目录 |
| git / .gitignore | ⚠️ 基本完成 | 仓库已存在（5 commit + remote）；`.gitignore` 已创建且实测生效（`git check-ignore Library/` 通过），**待提交** |
| 录屏工具 | ✅ 方案已定 | 漫游/效果类 = Recorder API 脚本驱动；UI 演示类 = obs-websocket 全自动（保底 Editor 预演 + OBS）（见 `04` §3.4）；`com.unity.recorder` 包 E01 安装 |
| Unity CLI / pipeline | 可选增强 | 2026-07-20 官方刚发布，beta 频道实验包；非必需，见 §4A |
| **视频管线启动条件** | ⚠️ 阻塞项 | ① 参考音色 `voice.wav`（用户提供：10-30s 清晰人声、无底噪、44.1kHz）② AutoVideo 引擎位置（BUILD.md 路径）——**E01 素材录完前必须就绪**，否则第一支视频无法构建 |

---

## 3. 总体技术架构（MyNature）

```
MyNature/
├── Assets/
│   ├── Audio/                      # ← 复制自源项目（42 wav + mixer）
│   ├── MyNature/
│   │   ├── Art/
│   │   │   ├── Materials/          # 38 mat（引用新 shader + 新贴图）
│   │   │   ├── Meshes/             # Blender MCP 导出的 FBX
│   │   │   ├── Models/             # Blender 源文件 .blend（备查）
│   │   │   ├── Textures/           # Python 生成（PNG，不用 PSD）
│   │   │   └── Terrain/            # TerrainData + TerrainLayer
│   │   ├── Prefabs/                # 树/草/石/花/环境/系统 prefab
│   │   ├── Scenes/                 # Demo + PineForest_Winter + 子 profile
│   │   ├── Scripts/
│   │   │   ├── Runtime/            # 9 个 MonoBehaviour（源 11 减 2 废弃雾脚本，适配 Unity 6）
│   │   │   └── Editor/             # 5 个 CustomEditor + Builder 批量装配脚本
│   │   ├── Settings/               # URP Asset / Renderer / Volume Profile
│   │   └── Shaders/                # 15 个手写 HLSL（URP 17.5 适配后）
│   │       └── Includes/           # 公共 .hlsl（风/雪/噪声全局变量封装）
├── tools/
│   ├── blender/                    # 已有：MCP addon + 启动脚本
│   ├── textures/                   # Python 贴图生成脚本
│   ├── scene_parser/               # Python：解析源 .unity YAML → 实例 Transform JSON（双场景）
│   └── video_lint.py               # 视频工程预检（clips 参数/meta/script 校验，见 04 §7）
├── docs/                           # 本文档系列
├── Packages/manifest.json          # 已有（Unity 6.5 依赖）
└── ProjectSettings/                # 已有，需改 Active Input Handling = Both
```

### 架构变化（相对源项目）

1. **新增 `Shaders/Includes/`**：snoise、风计算、雪计算抽成共享 `.hlsl`（原项目是逐 shader 复制粘贴）。
2. **新增 `Art/Models/.blend`**：保留 Blender 源文件。
3. **PSD → PNG**：程序化生成直接出 PNG。
4. **雾效重构成 Renderer Feature**：替代原 Built-in `OnRenderImage`。
5. **保留全局变量命名**：`WindStrenghtFloat`（拼写错误保留）等，C#↔shader 契约不变。
6. **新增 Editor Builder 脚本**：`MyNatureBuilder.cs` 等 MenuItem 命令，批量生成材质/prefab/地形撒点（替代旧计划的 Unity CLI 方案）。

---

## 4. 里程碑路线图

> 与教程集数的对应关系见 `03` 附录 D。阶段 0 因环境已就绪大幅简化。

### 阶段 0：工程骨架就位（预估 0.5 天）

**目标**：目录结构、音频、git、输入模式就绪。

- [ ] 0.1 建 `Assets/MyNature/` 目录骨架（Art/Prefabs/Scenes/Scripts/Settings/Shaders）
- [ ] 0.2 复制源项目 `Assets/IL3DN/Audio/` → `MyNature/Assets/Audio/`（42 wav + mixer）
- [ ] 0.3 `ProjectSettings → Player → Active Input Handling = Both`
- [ ] 0.4 提交 `.gitignore`（已创建并实测生效，Unity + Blender + Python 模板）；仓库已存在，无需 init
- [ ] 0.5 验证四链路：Unity 编译无错 / Blender MCP `get_scene_info` 有响应 / Python 出测试 PNG / URP Asset 配置检查（阴影、HDR、抗锯齿）
- [ ] 0.6 清理模板自带 TutorialInfo（可选）

**验收**：四链路全通，`.gitignore` 生效且工作区干净。

---

### 阶段 1：着色器核心系统（预估 3-4 天）⭐ 最关键

**目标**：URP 17.5 下 15 个着色器全部可编译，全局风/雪变量契约跑通。

- [ ] 1.1 `Shaders/Includes/IL3DN_Noise.hlsl`（snoise simplex）
- [ ] 1.2 `Shaders/Includes/IL3DN_Wind.hlsl`（风向/噪声采样/顶点位移）
- [ ] 1.3 `Shaders/Includes/IL3DN_Snow.hlsl`（雪累积 mask）
- [ ] 1.4 `Sky`（梯度天空盒，最简单的入门 shader）
- [ ] 1.5 `Branch`（第一个 PBR + 风，打通全局变量链路）
- [ ] 1.6 `Grass` / `Leaf` / `Pine`（风 + wiggle + 雪三系统）
- [ ] 1.7 `CardComplex` / `CardSimple` / `Cloud`（billboard 平面色系）
- [ ] 1.8 `Decals` / `SoftParticle` / `OpaqueSurface`
- [ ] 1.9 `Terrain`（4 splat，固定法线 `(0,0,1)`）
- [ ] 1.10 `Water`（5 pass 完整迁移，最复杂）
- [ ] 1.11 雾效 → **URP Full Screen Pass Renderer Feature**；删除 `IL3DN_Fog_PP` 概念
- [ ] 1.12 临时测试场景：每个 shader 一个球/平面逐一验证

**验收**：每个着色器有材质、测试场景可见正确效果；风/雪 global 变量改动能看到植被响应。

---

### 阶段 2：运行时脚本（预估 1 天）

**目标**：9 个运行时脚本（源 11 个减去 2 个废弃雾脚本）+ 5 个 Editor 脚本全部编译通过、Inspector 可调。

- [ ] 2.1 `IL3DN_Wind.cs`（保留拼写 `WindStrenght`）
- [ ] 2.2 `IL3DN_Snow.cs`
- [ ] 2.3 `IL3DN_ColorController.cs` + `IL3DN_ColorManagerEffects.cs` + `IL3DN_ColorManagerTextures.cs`
- [ ] 2.4 `IL3DN_SimpleFPSController.cs` + `IL3DN_SimpleMouseLook.cs`（Legacy Input，Both 模式下验证）
- [ ] 2.5 `IL3DN_ChangeWalkingSound.cs` + `IL3DN_PlayRandomSound.cs`
- [ ] 2.6 **删除** `IL3DN_Fog.cs` / `IL3DN_Fog_PP.cs`（被 Renderer Feature 取代）
- [ ] 2.7 5 个 Editor CustomEditor 脚本
- [ ] 2.8 `IL3DN_ColorManagerEffects` 的 `ShaderUtil` 用法适配 Unity 6 API

**验收**：编译无错，FPSController 可玩，Wind/Snow Inspector 可调参。

---

### 阶段 3：程序化贴图（预估 1.5 天）

**目标**：25+ 张贴图（对应源 25 PSD）全部由 `tools/textures/` Python 脚本生成。

> 教学拆分（2026-07-22 评审决策）：**最小贴图包 5 张（bark/leaf/grass/pine/windnoise）前置到 E04** 先把画面立住，避免 E03-E09 七集全是临时贴图；E10 再讲完整生成体系并全量替换。

- [ ] 3.0 `gen_minimal.py`（**最小贴图包 5 张**：bark/leaf/grass/pine/windnoise——E04 前置产出，先把画面立住；E10 全量替换）
- [ ] 3.1 `gen_bark.py`（树皮 ×2）
- [ ] 3.2 `gen_leaves.py`（叶 ×4）
- [ ] 3.3 `gen_grass.py`（草簇 ×4 alpha）
- [ ] 3.4 `gen_pine.py`（松针）
- [ ] 3.5 `gen_rock.py`
- [ ] 3.6 `gen_terrain.py`（dirt + grass tileable）
- [ ] 3.7 `gen_water.py`（法线贴图）
- [ ] 3.8 `gen_cloud.py`
- [ ] 3.9 `gen_fx.py`（GradientLinear/Radial/WhiteCircle/WhiteDot）
- [ ] 3.10 `gen_windnoise.py`（256² 风噪声）
- [ ] 3.11 `gen_petal.py`（花瓣 ×2）+ `gen_cards.py`（billboard 图集）+ `gen_groundsprites.py`
- [ ] 3.12 `generate_all.py` 统一入口

**验收**：全部 PNG 生成，导入 Unity 被材质引用，风格近似源项目。

---

### 阶段 4：3D 模型（Blender MCP）（预估 3-4 天）⭐ 工作量大

**目标**：41 个 FBX（源实测数）由 Blender MCP 生成，顶点色编码 `.g`=叶子权重、`.a`=风权重。

按 Demo 场景实例密度排优先级：

- [ ] 4.1 草簇 ×3（~8000 株，最高频）
- [ ] 4.2 枯叶/枯枝贴花 ×8（~5300 个）
- [ ] 4.3 岩石 ×3（~700 个）
- [ ] 4.4 花 ×3 + 睡莲 ×1
- [ ] 4.5 松树 ×3（完整 + `_OneMesh`）
- [ ] 4.6 桦树 ×3、灌木 ×3（含 `_OneMesh`）
- [ ] 4.7 山毛榉 ×3（复用 Birch 网格改色）
- [ ] 4.8 环境小物：GroundPatch、Human_scale、Water_Plane、LakeFog、Props_Bridge、云（plane+alpha）

> 建模策略：草/叶/花/睡莲 → 面片+alpha；树干 → 低模圆柱细分+顶点色；树冠 → 十字卡 billboard；岩石 → 细分立方+噪声扰动。每个模型出 `.blend` 源 + `.fbx`。

**验收**：FBX 导入 Unity 后 Mesh、材质槽、顶点色正确。

---

### 阶段 5：材质与预制体批量装配（Editor 脚本）（预估 1.5 天）

**目标**：38 材质 + 83 prefab（源实测数）由 Editor Builder 脚本一键生成。

- [ ] 5.1 `Scripts/Editor/MyNatureBuilder_Materials.cs`：`[MenuItem]` 批量建材质（shader + 贴图 + 颜色参数表驱动）
- [ ] 5.2 `Scripts/Editor/MyNatureBuilder_Prefabs.cs`：批量建 prefab（网格 + 材质 + LOD/碰撞配置）
- [ ] 5.3 变体批量派生：`_Card` / `_Dead` / `_Dead_Card` / `_OneMesh`
- [ ] 5.4 环境 prefab：Clouds、LakeFog、GodRays、WaterPlane、Bridge
- [ ] 5.5 系统 prefab：FPSController、Canvas、Weather、Post-Processing Volume、ChangeWalkingSound、PlayRandomSound

**验收**：菜单一键执行后 Prefabs/Materials 目录齐全，拖入场景正常显示。

---

### 阶段 6：地形与场景实例精确还原（预估 1 天）

**目标**：250×250 米地形 + 2049² 高度图 + 2 TerrainLayer + **按源场景 YAML 逐实例精确摆放 1.6 万实例**。

> 2026-07-22 评审决策：放弃"密度图+随机种子近似撒点"。源场景 YAML 里有全部 16235 个实例的精确 Transform——有精确答案不用近似解。保真度"逐实例一致"，100% 可自动化/可重跑/可对账。

- [ ] 6.1 `tools/scene_parser/`（Python）：解析源 `.unity` YAML → 输出 `{prefab名, position, rotation, scale}` JSON（Demo 16235 / Winter 8087 两套）
- [ ] 6.2 高度图：读取源 `IL3DN_Terrain_Height.raw`（2049×2049×16bit，数据资产允许复用）→ 按需重采样导入；Terrain 世界尺寸 250×250，位置 `(-125,-5.5,-125)`
- [ ] 6.3 2 个 TerrainLayer（Grass/Dirt）+ `IL3DN/Terrain` shader 绑定
- [ ] 6.4 `MyNatureBuilder_Terrain.cs`：读 JSON 逐实例 Instantiate/摆放（⚠️ 草的实现形态——Terrain Detail 还是场景实例——以 05 技术规格对源 TerrainData 的查证结论为准；场景实例则全走 JSON 摆放）
- [ ] 6.5 重点资产（桥/水面/睡莲）同走 JSON 坐标；撒点算法仍作教学知识点保留（E15），但产出用精确数据

**验收**：实例计数与源场景对账一致（16235 ± 允许差异）；玩家可走动；同机位截图可比。

---

### 阶段 7：Demo 场景装配与调试（预估 1.5 天）

- [ ] 7.1 顶层结构（Audio / ColorManager / Enviro / Terrain / Settings / Weather / Light）
- [ ] 7.2 5 个 AudioSource（Music/Birds/Wind/Water/Steps）
- [ ] 7.3 Directional Light：暖色 `(1,0.957,0.839)`，欧拉 `(46,265,0)`，Soft Shadows
- [ ] 7.4 Volume：Bloom + ShadowsMidtonesHighlights（参考 Demo profile）
- [ ] 7.5 RenderSettings 雾：Exp2 `(0.431,0.494,0.718)`
- [ ] 7.6 Weather：WindStrenght=0.8 / WindSpeed=0.4 / WindTurbulence=0.1 / LeavesWiggle=1 / GrassWiggle=0.5；Snow=0
- [ ] 7.7 ColorManager 四季调色板
- [ ] 7.8 Occlusion Culling（250×50×250）
- [ ] 7.9 FPSController 漫游测试
- [ ] 7.10 视觉调试（透明排序、阴影、风/雪响应）

**验收**：场景可玩，视觉接近源 Demo。

---

### 阶段 8：冬季松林场景（预估 0.5 天）

**目标**：按源 `TheIllustratedNature_PineForest_Winter.unity`（31 MB YAML，8087 实例）精确还原冬季场景。

> 2026-07-22 复核更正：冬季场景源文件**存在且完整**，从"原创组装"升级为"与 Demo 同级的精确解析还原"——复用阶段 6 的 scene_parser + Builder 管线，工作量与风险同步大降。

- [ ] 8.1 scene_parser 解析 Winter 场景 → 8087 实例 JSON（树种分布、Transform、天气/灯光参数）
- [ ] 8.2 地形引用 `IL3DN_Terrain_02` 结构复刻 + 实例按 JSON 摆放
- [ ] 8.3 Weather 参数按 YAML 还原：Snow=1，各 `SnowXxxFloat` 与 `AlphaCutoffFloat` 取源值
- [ ] 8.4 ColorManager 切冬季 slot（取源场景 slot 值）
- [ ] 8.5 冬季 Volume Profile（按源 `IL3DN_PPProfile_PineForest_Winter.asset` 解析值）
- [ ] 8.6 冷色灯光 + 雾色按 YAML 还原
- [ ] 8.7 与源 Winter 同机位截图对比（YAML 中提取源相机参数）
- [ ] 8.8 脚步声：源无雪地音效，ChangeWalkingSound 冬季区域用 Dirt/Grass 组代替

**验收**：冬季场景可漫游，雪覆盖完整；实例计数对账（8087 ± 允许差异）；两场景体现季节系统能力。

---

### 阶段 9：调优与验收（预估 1 天）

- [ ] 9.1 Profiler（目标 60fps @ 1080p 中端机）
- [ ] 9.2 GPU Instancing / LOD / Billboard Distance 调参
- [ ] 9.3 **量化验收**（替代"视觉接近"的主观描述）：
  - 同机位截图对比：`MyNatureBuilder_Screenshots.cs` 出固定机位组 + Python 算 SSIM/直方图距离，阈值 SSIM ≥ 0.75 通过（逐阶段可收紧）
  - 对账指标：实例数 vs 源 YAML、材质/prefab 数 vs 38/83、shader 编译 0 error、全局变量名逐一 diff
  - borderline 案例人眼终审
- [ ] 9.4 修已知 bug
- [ ] 9.5 README.md（项目结构、运行方式、工具链说明）

---

## 4A. 端到端自动化架构（2026-07-22 调研定稿）

> 回答一个问题：**不用 Unity CLI，是不是就必须手动操作编辑器？——不是。**
> 自动化的核心从来不是某个 CLI，而是"业务逻辑写在 Editor 脚本里 + 触发方式可插拔"。

### 4A.1 三层架构

```
┌─ L2 编排层：AI Agent（Trae）或 PowerShell 脚本，按里程碑顺序串起所有步骤
│
├─ L1 触发层（四选一或可叠加，与 L0 解耦）：
│   ① Unity.exe -batchmode -executeMethod MyNature.Builder.Xxx -quit
│      ← 保底主力。Unity 自带、零依赖、无头运行、CI 级稳定
│   ② Unity CLI + com.unity.pipeline（unity command run/eval）
│      ← 2026-07-20 官方发布，beta 实验包；驱动运行中的 Editor，免逐次域重载
│   ③ 官方 Unity MCP Server（com.unity.ai.assistant）
│      ← 需 Unity Cloud 连接 + Unity AI 订阅；MCP 工具化驱动 Editor
│   ④ 社区 unity-mcp（WebSocket 插件，免费）
│      ← 与 Blender MCP 同形态，AI Agent 直连运行中的 Editor
│
├─ L0.5 验证层（2026-07-22 评审新增）：每步跑完机器判定成功，否则中断编排
│   · 结构化结果：每个 Builder 方法返回 {生成数量, 失败清单}，batchmode 退出码非 0 即停
│   · 编译门禁：每集结束 -batchmode -quit 空跑（编译错误即 fail）+ 可选 EditMode 测试
│   · 视觉门禁：固定机位截图 + Python SSIM/直方图对比（阈值见阶段 9.3）
│   · 对账门禁：资产计数 vs 05/06/07 规格清单（38 mat / 83 prefab / 16235+8087 实例）
│
└─ L0 业务层：Editor 脚本（C# static 方法）
    MyNatureBuilder_Materials / _Prefabs / _Terrain / _Screenshots / _Recorder
    ← 所有"干活"的代码只写在这里，四种触发器调的都是同一套方法
```

> 没有 L0.5，"一条 PowerShell 顺序调 N 次 batchmode"就是裸奔——中间一步静默失败，后面全白跑。

### 4A.2 各环节自动化率评估

| 环节 | 自动化方式 | 自动化率 |
|---|---|---|
| 目录骨架 / 音频复制 / git | PowerShell 脚本 | 100% |
| Shader / C# 代码编写 | AI Agent 直接写文件 + batchmode 编译验证 | ~100% |
| 贴图生成 | Python 脚本 | 100% |
| 3D 模型 | Blender MCP（已就绪） | ~95%（个别造型需人工微调） |
| 材质 / Prefab / 变体 | L0 Builder 脚本 + batchmode 触发 | 100% |
| 地形创建 / 高度图导入 | L0 Builder 脚本 + batchmode 触发 | 100% |
| **场景实例还原（双场景）** | `tools/scene_parser`（YAML→JSON）+ L0 Builder 按 JSON 摆放 | **100%**（逐实例精确，可对账） |
| 场景装配参数还原（双场景） | L0 Builder 脚本（参数全部来自调研报告 + 场景 YAML 解析） | ~98% |
| **视觉校色 / 调优** | 截图对比工具（SSIM/直方图）自动判分 → 人只终审 borderline | **~75%**（有工具支撑，终审保留人工） |
| 静态截图素材 | RenderTexture / ScreenCapture（batchmode 带 GPU，不加 `-nographics`） | 100% |
| 漫游/效果类录屏 | Unity Recorder API（RecorderController）+ 代码驱动相机路径 → 直接出 1080p30 mp4 | ~90% |
| 编辑器 UI 操作类录屏 | obs-websocket 全自动（Editor 演示驱动器 + WebSocket 启停录制，见 `04` §3.4） | ~90%（人工按键降为备选） |
| 视频构建 | AutoVideo 构建 Agent（BUILD.md 流程）+ `tools/video_lint.py` 预检 | 100% |

### 4A.3 结论

- **工程复刻**：全链路无人值守，人工判断收敛为两处——视觉校色 borderline 终审、模型个别微调。一批次跑通 ≈ 一条 PowerShell 总控脚本顺序调 N 次 batchmode + L0.5 门禁逐步验收。
- **视频素材**：静态截图与漫游录屏脚本化自动产出；UI 操作演示镜头经 obs-websocket 也可全自动，人从"逐条录制"降为"成片验收"。
- **Unity CLI 的定位**：2026-07-20 官方刚发布（Unite Seoul），`com.unity.pipeline` 仍是 beta 实验包。优势是驱动**运行中的** Editor、免去每次 batchmode 的启动与域重载开销，适合高频交互式调试。**作为可选加速，不作为依赖**；E01 时可花 10 分钟试装，好用就留，不好用不影响任何计划。
- **Unity MCP（官方/社区）**：与 Blender MCP 同形态，适合"Agent 边做边看"的交互式装配；同样可选，安装与否不改变 L0 脚本设计。

> 教学侧说明：E14 按 L0（Builder 脚本）+ ①（batchmode）为主线教学，②③④ 作为"进阶彩蛋"在集尾提一句，避免观众环境差异导致跟做失败。

---

## 5. 时间与工作量估算

| 阶段 | 工作量（人天） | 备注 |
|---|---|---|
| 0 工程骨架 | 0.5 | 环境已就绪，大幅简化 |
| 1 着色器 ⭐ | 3-4 | URP 17.5 API 迁移是难点 |
| 2 脚本 | 1 | 直接移植为主 |
| 3 贴图 | 1.5 | Python 自动化（源实测 25 PSD，量比预估少） |
| 4 模型 ⭐ | 3-4 | Blender MCP 数量多（41 FBX） |
| 5 材质/Prefab | 1 | Editor Builder 脚本（38 mat + 83 prefab，比预估少 ~15%） |
| 6 地形+实例还原 | 1 | scene_parser + Builder（精确还原替代撒点设计，更省） |
| 7 Demo 装配 | 1.5 | |
| 8 冬季场景 | 0.5 | 源场景存在，复用阶段 6 管线精确还原 |
| 9 调优 | 1 | 含量化验收工具 |
| **合计** | **~14-16 人天** | 不含视频制作 |

> **关键路径**：阶段 1（着色器）→ 阶段 4（模型）→ 阶段 6/7（场景）→ 阶段 8（冬季）。阶段 3（贴图）与阶段 2（脚本）可与 1/4 部分并行。

---

## 6. 风险与备选方案

| 风险 | 概率 | 备选 |
|---|---|---|
| URP 17.5 着色器 API 迁移卡壳 | 中 | 单 shader 退化为 URP Simple Lit + tint，先保流程后补特效；参考源项目自带 URP unitypackage 的官方写法（见 01 §10） |
| Blender MCP 长脚本超时（180s） | 中 | 拆小脚本分批执行；退化手写 bpy 文件本地跑 |
| 1.6 万实例性能不足 | 低 | 精确还原本身就接近源性能；GPU Instancing；`_OneMesh` 变体；必要时降密度 |
| 程序化贴图风格不像 | 中 | 阶段 9 调色；CC0 手绘贴图补关键几张 |
| Water 5 pass 太复杂 | 中 | 砍 ShadowCaster/DepthOnly 只留 Forward（牺牲水下深度） |
| 旧 Input Manager 在 Both 模式下行为差异 | 低 | 切回仅 Input Manager（模板 Input System 包可留不用） |
| 顶点色约定逐 shader 确认不通过 | 低 | Blender 涂色脚本参数化，按 05 通道表重涂重导（成本集中在建模前查证） |
| 草的实现形态与计划不符 | 低 | 05 规格先查证源 TerrainData；场景实例则全走 JSON 摆放，工作量相当 |

---

## 7. 文档地图

| 文档 | 作用 | 状态 |
|---|---|---|
| `01_源项目调研报告.md` | 源项目逐项分析 | ✅ 已修订②（2026-07-22 评审更正） |
| `02_复刻计划与里程碑.md` | 本文档：工程视角阶段计划 | ✅ 本版 |
| `03_教程大纲_逐集产出.md` | ⭐ 教学主线（每集 = 一集视频） | ✅ 已修订② |
| `04_视频制作规范_AutoVideo.md` | 每集视频的 AutoVideo 产出规范 | ✅ 已修订② |
| `REVIEW_2026-07-22_文档评审意见.md` | 本轮修订的依据（实测复核记录） | ✅ 已处理归档 |
| `05_技术规格_脚本与Shader.md` | 移植字段/API 速查 + **逐 shader 顶点色通道表** + 草实现形态查证 | ⬜ **滚动编写：E03 开工前完成 shader 部分** |
| `06_模型清单_Blender任务.md` | Blender 建模任务明细（含 FBX 导出参数固化） | ⬜ **滚动编写：E04 开工前完成** |
| `07_贴图规格_Python任务.md` | 贴图生成规格明细 | ⬜ **滚动编写：E04 最小贴图包前完成对应部分，E10 前补齐** |

> **优先级**：`03`（教程大纲）是教学主线，`02`（本文档）是工程视角。两者阶段编号有映射（`03` 附录 D），冲突时以 `03` 为准。
> **规格文档节奏**（2026-07-22 评审决策）：05/06/07 是 AI Agent 自动化执行的**输入**，不是事后记录——一律"提前一集完成"，每集 checklist 首项即检查所需规格章节已就绪。

---

*下一步：规划确认后，从教程 **E01**（≈ 阶段 0）开始执行。每集结束 commit + tag，视频按 `04` 规范产出。*
