# MyNature 任务拆分与执行路线

> 版本：2026-07-22 封版后
> 依据：`01` 调研报告（封版）、`02` 复刻计划（封版）、`03` 教程大纲（封版）、`04` 视频规范（封版）
> 用途：把整体目标拆成可独立执行、可验证的最小工作包，指导自动化开发。

---

## 0. 环境基线（调研结论）

| 项 | 状态 | 说明 |
|---|---|---|
| Unity 6000.5.4f1 + URP 17.5 | ✅ | `C:\Program Files\Unity\Hub\Editor\6000.5.4f1` |
| Blender 4.2 + MCP addon | ✅ | `C:\Program Files\Blender Foundation\Blender 4.2`；`tools/blender/` 就绪 |
| Python 3.12.10 + Pillow + numpy | ✅ | Pillow 12.2.0, numpy 2.4.4, requests 2.33.1 |
| ffmpeg 8.1.1 | ✅ | 全局可用 |
| GPU RTX 5090 | ✅ | 带 GPU batchmode 可渲染 |
| OBS | ❌ 待安装 | E01 前安装 28+ 版（含 obs-websocket） |
| Unity Recorder 包 | ❌ 未装 | `com.unity.recorder` 未在 manifest 中，E01 安装 |
| MyNature 项目 | 空白模板 | 无 Audio/无目录骨架/无自定义 shader/无自定义 cs |
| git + .gitignore | ⚠️ | `.gitignore` 已创建未提交；其余已 commit |
| G 盘空间 | 416 GB 剩余 | 充足 |

---

## 1. 任务总览

> 任务编号规则：`T{阶段}-{序号}` 为工程任务；`V{两位集号}` 为视频任务；`T8-x` 为贯穿自动化工具。
> 依赖箭头：`→` 表示严格先后；`↔` 表示可并行。

```
T0-1 骨架就位 ──→ T0-2 四链路验证 ─────────────────────────────┐
                                                              │
T1-0 规格前置 → T1-1 Include ──→ T1-2 Sky ──→ T1-3 Branch ──→ T1-4 Grass/Leaf/Pine
                                    ↓                           ↓
                              T1-5 卡片辅助 → T1-6 Terrain      T1-7 Water
                                    ↓                           ↓
                              T1-8 Fog Feature ←─────────────── T1-9 全量编译
                                                              │
T2-1 Wind/Snow ←──────────────────────────────────────────────┤
T2-2 FPS+音频 ←───────────────────────────────────────────────┤
T2-3 ColorManager ←───────────────────────────────────────────┤
T2-4 Editor+编译 ←────────────────────────────────────────────┘

T3-0 贴图规格前置 → T3-1 最小贴图包（E04 用）
                                    ↓
                              T3-2 完整贴图 → T3-3 全量替换

T4-0 模型规格前置 → T4-1 Pine_01 → T4-2 批量草花岩 → T4-3 树族 → T4-4 环境小物

T5-1 Builder_Materials ──→ T5-2 Builder_Prefabs ──→ T5-3 batchmode 验证

T6-0 scene_parser ──→ T6-1 Demo 地形还原 ──→ T6-2 Demo 装配
                                    ↓
                              T6-3 Winter 还原 ──→ T6-4 对比验证

T7-1 性能调优 ──→ T7-2 量化验收

贯穿工具（与阶段并行）：
  T8-1 Builder_Screenshots  T8-2 Builder_Recorder  T8-3 SSIM对比脚本  T8-4 video_lint

视频（每集独立，与工程并行）：
  V01 → V02 → V03 → V04 → V05 → V06 → V07 → V08 → V09 → V10
  → V11 → V12 → V13 → V14 → V15 → V16 → V17 → V18
```

---

## 2. 工程任务详情

### 阶段 0：工程骨架（对应 E01）

---

#### T0-1 目录骨架与音频就位
- **阶段**：0 / E01
- **实现方向**：
  1. 建 `Assets/MyNature/` 目录骨架（Art/Materials/Meshes/Models/Textures/Terrain、Prefabs、Scenes、Scripts/Runtime、Scripts/Editor、Settings、Shaders/Includes）
  2. 复制源 `Assets/IL3DN/Audio/`（42 wav + 1 mixer）→ `Assets/Audio/`，`Jumnp` 拼写错误原样保留
  3. 提交 `.gitignore`（已创建）
- **产出物**：完整目录树 + `Assets/Audio/` 全部文件 + `.gitignore` 提交
- **验证方式**：`git status` 工作区干净；`Get-ChildItem Assets/Audio` 计数 = 43（42 wav + 1 mixer）
- **自动化方式**：AI Agent 自动（PowerShell 脚本）
- **依赖**：无
- **工时**：0.5 h

---

#### T0-2 项目配置与四链路验证
- **阶段**：0 / E01
- **实现方向**：
  1. `ProjectSettings → Player → Active Input Handling = Both`
  2. Packages/manifest.json 追加 `com.unity.recorder`
  3. 四链路验证：Unity 编译无错 / Blender MCP `get_scene_info` 有响应 / Python `gen_test_gradient.py` 出 PNG / URP Asset 配置检查（阴影 Soft、HDR on、MSAA 4x）
  4. 清理 `TutorialInfo`
- **产出物**：配置变更 + 测试 PNG + 验证报告
- **验证方式**：batchmode `-quit` 编译通过；Blender MCP 返回 scene_info；Python PNG 可打开
- **自动化方式**：AI Agent 自动
- **依赖**：T0-1
- **工时**：0.5 h

---

### 阶段 1：着色器核心系统（对应 E03/E05/E07/E09/E11/E12）

---

#### T1-0 前置：05 技术规格（shader 部分 + 顶点色通道表）
- **阶段**：前置 / E03 开工前必须完成
- **实现方向**：
  1. 逐 shader API 对照表：源 URP 10 API → URP 17.5 API（`UniversalFragmentPBR`、`GetMainLight`、include 路径等）
  2. 逐 shader 顶点色通道使用表：确认每个 shader 读了 `.g/.a/.r/.b` 哪些通道（Pine 已初步排除 `.rgb` 为输出色）
  3. 草实现形态查证：解析源 TerrainData 二进制确认草是 PrefabInstance 还是 Terrain Detail
  4. 与官方 PDF 交叉验证关键约定
- **产出物**：`docs/05_技术规格_脚本与Shader.md`（shader 章节）
- **验证方式**：对照表覆盖全部 15 shader；顶点色表经逐 shader grep 确认；TerrainData 查证有结论
- **自动化方式**：AI Agent 自动（grep + 文档撰写）
- **依赖**：无（独立前置）
- **工时**：4 h

---

#### T1-1 Include 公共库（Noise + Wind + Snow）
- **阶段**：1 / E05
- **实现方向**：
  1. `Shaders/Includes/IL3DN_Noise.hlsl`：snoise simplex 3D（从源 shader 提取，统一到一个 include）
  2. `Shaders/Includes/IL3DN_Wind.hlsl`：世界空间 noise 平移 → 顶点位移函数；全局变量读取封装
  3. `Shaders/Includes/IL3DN_Snow.hlsl`：雪累积 mask（`saturate(pow(WorldNormal.y, k)) * SnowXxxFloat`）
- **产出物**：3 个 `.hlsl` 文件
- **验证方式**：能被其他 shader `#include`；batchmode 编译通过（写一个空测试 shader 引用它们）
- **自动化方式**：AI Agent 自动
- **依赖**：T1-0
- **工时**：3 h

---

#### T1-2 Sky shader
- **阶段**：1 / E03
- **实现方向**：URP Unlit shader：世界空间视线方向 → Y 轴双色渐变 + 地平线柔化 + HDR 曝光
- **产出物**：`Shaders/IL3DN_Sky.shader` + 材质
- **验证方式**：Game 视图天空可见；颜色可调；无编译错误
- **自动化方式**：AI Agent 自动
- **依赖**：T1-0
- **工时**：2 h

---

#### T1-3 Branch shader
- **阶段**：1 / E05
- **实现方向**：URP PBR + 风位移（读 `IL3DN_Wind.hlsl`）+ Snow mask（读 `IL3DN_Snow.hlsl`）+ `[Toggle(_WIND_ON)]`
- **产出物**：`Shaders/IL3DN_Branch.shader` + 材质
- **验证方式**：Branch 材质挂到 Pine 模型 → 风滑杆可调；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T1-1
- **工时**：4 h

---

#### T1-4 Grass / Leaf / Pine shaders
- **阶段**：1 / E07
- **实现方向**：
  - Grass：UV rotation wiggle + 风 + Snow
  - Leaf：+ alpha cutoff 全局调节
  - Pine：风 + wiggle + 雪三合一（最复杂植被 shader）
  - keyword：`_WIND_ON` / `_WIGGLE_ON` / `_SNOW_ON`
- **产出物**：3 个 shader + 材质
- **验证方式**：3 种植被全部换装 → 风/雪双系统 Inspector 可调；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T1-3
- **工时**：6 h

---

#### T1-5 卡片系 + 辅助 shaders
- **阶段**：1 / E09
- **实现方向**：
  - `CardComplex`：按 `.g` 通道 lerp bark→leaf
  - `CardSimple` / `Cloud`：纯 alpha cutout
  - `Decals`：snoise 双色混合
  - `SoftParticle`：深度 fade
  - `OpaqueSurface`：岩石 + 雪
- **产出物**：5 个 shader + 材质
- **验证方式**：各材质测试可见效果；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T1-4（复用 Snow.hlsl）
- **工时**：5 h

---

#### T1-6 Terrain shader
- **阶段**：1 / E09
- **实现方向**：4 splat 混合 + 固定法线 `(0,0,1)` + Snow mask
- **产出物**：`Shaders/IL3DN_Terrain.shader` + 材质
- **验证方式**：TerrainLayer 绑定后地形可见正确颜色；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T1-5
- **工时**：3 h

---

#### T1-7 Water shader
- **阶段**：1 / E11
- **实现方向**：5 pass（Forward / ShadowCaster / DepthOnly / Meta / 可选）→ 深度差三板斧（泡沫/边缘色/深水层）+ 双 snoise panner + 双法线 panner + 双 Fresnel
- **产出物**：`Shaders/IL3DN_Water.shader` + 材质
- **验证方式**：水面可见泡沫/深度/Fresnel；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T1-6
- **工时**：6 h（最复杂 shader）

---

#### T1-8 Fog Renderer Feature
- **阶段**：1 / E12
- **实现方向**：`ScriptableRendererFeature` + Full Screen Pass shader；深度雾三分色（Near/Far/Glow）
- **产出物**：`IL3DN_FogFeature.cs` + 全屏雾 shader + Renderer 配置
- **验证方式**：挂到 PC_Renderer → 场景纵深雾可见；与源雾效对比
- **自动化方式**：AI Agent 自动
- **依赖**：T1-7
- **工时**：4 h

---

#### T1-9 着色器全量编译验证
- **阶段**：1 / —
- **实现方向**：batchmode 跑空项目编译全部 15 shader + 3 include；输出编译日志
- **产出物**：编译通过报告（0 error / 0 warning 目标）
- **验证方式**：`Unity.exe -batchmode -projectPath . -quit` 退出码 0；日志无 shader error
- **自动化方式**：AI Agent 自动
- **依赖**：T1-1 ~ T1-8 全部完成
- **工时**：0.5 h

---

### 阶段 2：运行时脚本（对应 E13）

---

#### T2-1 Wind / Snow 系统
- **阶段**：2 / E05+E07
- **实现方向**：
  - `IL3DN_Wind.cs`：`[ExecuteInEditMode]` 全局风驱动器，保留 `WindStrenght` 拼写
  - `IL3DN_Snow.cs`：6 类表面独立雪量 global
- **产出物**：2 个运行时脚本
- **验证方式**：Inspector 可调参；shader 响应 global 变量变化；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T1-3（Branch shader 需 Wind global）
- **工时**：2 h

---

#### T2-2 FPS 控制器 + 音频系统
- **阶段**：2 / E13
- **实现方向**：
  - `SimpleFPSController`：CharacterController + Legacy Input（`Both` 模式）+ 脚步声
  - `SimpleMouseLook`：纯 Serializable 类
  - `ChangeWalkingSound`：trigger 切脚步材质音
  - `PlayRandomSound`：周期随机环境音
- **产出物**：4 个运行时脚本
- **验证方式**：Play 模式可第一人称漫游；脚步随地面变化；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T0-2（`Both` 模式）
- **工时**：3 h

---

#### T2-3 ColorManager 系统
- **阶段**：2 / E13
- **实现方向**：
  - `ColorController`：slot 持有者
  - `ColorManagerEffects`：季节调色板（`ShaderUtil` Unity 6 适配）
  - `ColorManagerTextures`：颜色+贴图季节切换
- **产出物**：3 个运行时脚本
- **验证方式**：slot 切换颜色变化；Inspector 可编辑；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T2-2
- **工时**：2 h

---

#### T2-4 CustomEditor + 编译验证
- **阶段**：2 / E13
- **实现方向**：5 个 CustomEditor（ColorController / ColorManagerEffects / ColorManagerTextures / Snow / Wind）
- **产出物**：5 个 Editor 脚本
- **验证方式**：Inspector 有装饰 header；batchmode 编译通过
- **自动化方式**：AI Agent 自动
- **依赖**：T2-1 ~ T2-3
- **工时**：2 h

---

### 阶段 3：程序化贴图（对应 E04 最小包 + E10 全量）

---

#### T3-0 前置：07 贴图规格
- **阶段**：前置 / E04 开工前
- **实现方向**：
  1. 逐张贴图规格：尺寸、色值、alpha 设计、tileable 边界处理
  2. 最小贴图包 5 张优先定义（bark/leaf/grass/pine/windnoise）
  3. 与源 PSD 像素级色值对照（取关键色样）
- **产出物**：`docs/07_贴图规格_Python任务.md`
- **验证方式**：规格覆盖源 25 PSD；最小包 5 张有明确尺寸+色值
- **自动化方式**：AI Agent 自动
- **依赖**：无
- **工时**：3 h

---

#### T3-1 最小贴图包 5 张
- **阶段**：3 / E04
- **实现方向**：`tools/textures/gen_minimal.py`：出 bark/leaf/grass/pine/windnoise 各 1 张
- **产出物**：5 PNG + 生成脚本
- **验证方式**：导入 Unity 被 Pine_01 材质引用后画面有插画感；文件存在
- **自动化方式**：AI Agent 自动
- **依赖**：T3-0
- **工时**：2 h

---

#### T3-2 完整贴图生成体系
- **阶段**：3 / E10
- **实现方向**：
  1. `tools/textures/` 各分类脚本（bark/leaves/grass/pine/rock/terrain/water/cloud/fx/windnoise/petal/cards/groundsprites）
  2. `generate_all.py` 统一入口
- **产出物**：10+ 脚本 + 25+ PNG
- **验证方式**：`generate_all.py` 一键出图无错；全部 PNG 导入 Unity
- **自动化方式**：AI Agent 自动
- **依赖**：T3-1（最小包是子集）
- **工时**：6 h

---

#### T3-3 全量替换验证
- **阶段**：3 / E10
- **实现方向**：用新 PNG 替换 E03-E09 所有临时贴图与最小包；场景同机位对比
- **产出物**：替换后的材质引用更新 + 对比截图
- **验证方式**：无 pink 材质（贴图丢失）；同机位 SSIM 提升
- **自动化方式**：AI Agent 自动（Editor 脚本批量替换材质引用）
- **依赖**：T3-2 + T1-9（shader 全编译通过）
- **工时**：1 h

---

### 阶段 4：3D 模型（对应 E04/E06/E08）

---

#### T4-0 前置：06 模型规格
- **阶段**：前置 / E04 开工前
- **实现方向**：
  1. 41 个 FBX 的完整清单（名称、变体规则、顶点色涂色方案）
  2. FBX 导出参数固化（Forward=-Z, Up=Y, Apply Scalings=FBX Units Scale）
  3. 顶点色通道表最终确认（基于 T1-0 的逐 shader 查证结论）
  4. 每个模型的尺寸/多边形预算
- **产出物**：`docs/06_模型清单_Blender任务.md`
- **验证方式**：清单覆盖全部 41 FBX；导出参数可脚本化；顶点色确认无返工风险
- **自动化方式**：AI Agent 自动
- **依赖**：T1-0（顶点色通道表）
- **工时**：3 h

---

#### T4-1 Pine_01 首棵建模
- **阶段**：4 / E04
- **实现方向**：
  1. `tools/blender/gen_pine.py`：圆柱树干 + 锥形树冠 + 顶点色涂色（g=1, a=0.8）
  2. MCP 执行 → 导出 `Pine_01.fbx`
  3. Unity 导入 + 最小贴图包换装
- **产出物**：`Pine_01.blend` + `Pine_01.fbx` + gen 脚本
- **验证方式**：FBX 导入后 Mesh/材质槽/顶点色正确；画面有插画感
- **自动化方式**：AI Agent 自动（MCP 调用 bpy）
- **依赖**：T3-1（最小贴图包）+ T4-0
- **工时**：2 h

---

#### T4-2 批量草花岩建模
- **阶段**：4 / E06
- **实现方向**：
  1. 扩展 `gen_foliage.py`：配置表驱动 → Grass_01/02/03、Flower_01/02/03、Rock_01/02/03
  2. 批量 MCP 执行 → 9 个 FBX
- **产出物**：`gen_foliage.py` + 9 FBX + .blend
- **验证方式**：全部 FBX 导入 Unity，材质槽正确
- **自动化方式**：AI Agent 自动（分批执行防超时）
- **依赖**：T4-1（流程验证通过）
- **工时**：4 h

---

#### T4-3 树族建模
- **阶段**：4 / E08
- **实现方向**：
  1. Birch_01/02/03（含 `_OneMesh`）
  2. Shrub_Round_01/02/03（含 `_OneMesh`）
  3. Beech_01/02/03（复用 Birch 网格）
- **产出物**：24 个新 FBX
- **验证方式**：全部导入 + 换装 E07 shader → 树林陈列场景风吹可见
- **自动化方式**：AI Agent 自动
- **依赖**：T4-2
- **工时**：4 h

---

#### T4-4 环境小物建模
- **阶段**：4 / —
- **实现方向**：GroundPatch、Human_scale、Water_Plane、LakeFog、Props_Bridge、云（plane+alpha）
- **产出物**：~8 个 FBX
- **验证方式**：导入后可直接进场景
- **自动化方式**：AI Agent 自动
- **依赖**：T4-3
- **工时**：2 h

---

### 阶段 5：Builder 批量装配（对应 E14）

---

#### T5-1 Builder_Materials
- **阶段**：5 / E14
- **实现方向**：`Scripts/Editor/MyNatureBuilder_Materials.cs`：表驱动，shader + 贴图 + 颜色参数 → 循环 `new Material` + `AssetDatabase.CreateAsset`
- **产出物**：38 个 .mat + Builder 脚本
- **验证方式**：菜单执行后 Materials 目录 38 个文件；拖入场景正常显示；batchmode 触发也成功
- **自动化方式**：AI Agent 自动
- **依赖**：T1-9（shader 全编译）+ T3-3（贴图就绪）+ T4-0（材质参数表来自规格）
- **工时**：3 h

---

#### T5-2 Builder_Prefabs + 系统 prefab
- **阶段**：5 / E14
- **实现方向**：
  1. `Scripts/Editor/MyNatureBuilder_Prefabs.cs`：网格+材质装配 + 变体派生（`_Card` / `_Dead` / `_Dead_Card` / `_OneMesh`）
  2. 系统 prefab：FPSController、Canvas、Weather、Post-Processing Volume、ChangeWalkingSound、PlayRandomSound
- **产出物**：83 prefab + Builder 脚本 + 系统 prefab
- **验证方式**：Prefabs 目录齐全；抽查拖场景正常
- **自动化方式**：AI Agent 自动
- **依赖**：T5-1 + T4-4（全部 FBX 就绪）+ T2-4（脚本编译通过）
- **工时**：4 h

---

#### T5-3 batchmode 验证
- **阶段**：5 / E14
- **实现方向**：`Unity.exe -batchmode -projectPath . -executeMethod MyNature.Builder.BuildAll -quit` 跑 Materials + Prefabs + 系统
- **产出物**：batchmode 成功日志
- **验证方式**：退出码 0；资产计数对账（38 mat / 83 prefab / 系统 prefab 齐全）
- **自动化方式**：AI Agent 自动
- **依赖**：T5-2
- **工时**：0.5 h

---

### 阶段 6：场景精确还原（对应 E15/E16/E17）

---

#### T6-0 scene_parser 开发
- **阶段**：6 / E15
- **实现方向**：
  1. `tools/scene_parser/parse.py`：解析源 `.unity` YAML → 提取 `PrefabInstance` 的 `m_SourcePrefab` + `m_Modifications`（position/rotation/scale）
  2. 输出 `demo_instances.json`（16235 条）+ `winter_instances.json`（8087 条）
  3. 异常处理：嵌套/缺 Transform 的实例输出到 `exceptions.json`
- **产出物**：parser 脚本 + 2 个 JSON + exceptions 清单
- **验证方式**：JSON 条目数 = 16235 + 8087；抽样 10 条与 YAML 原文核对；对账容差 ±0.5%
- **自动化方式**：AI Agent 自动
- **依赖**：无（纯 Python 工具，不依赖 Unity）
- **工时**：4 h

---

#### T6-1 Demo 地形还原
- **阶段**：6 / E15
- **实现方向**：
  1. 读取源 `IL3DN_Terrain_Height.raw`（2049×2049×16bit）→ 重采样导入 TerrainData
  2. Terrain 世界尺寸 250×250，位置 `(-125,-5.5,-125)`
  3. 2 个 TerrainLayer（Grass/Dirt）绑定 `IL3DN_Terrain` shader
- **产出物**：TerrainData.asset + 2 TerrainLayer
- **验证方式**：Terrain 有起伏；与源场景同位置
- **自动化方式**：AI Agent 自动（Editor 脚本）
- **依赖**：T6-0 + T1-6（Terrain shader）
- **工时**：2 h

---

#### T6-2 Demo 场景装配（E16）
- **阶段**：6 / E16
- **实现方向**：
  1. `MyNatureBuilder_Terrain.cs`：读 `demo_instances.json` 逐实例 Instantiate/摆放
  2. 顶层 7 大对象装配（Audio/ColorManager/Enviro/Terrain/Settings/Weather/Light）
  3. 参数还原：灯光、雾、Volume、Weather、ColorManager 四季调色板
  4. Occlusion Culling 烘焙
- **产出物**：完整 `Demo.unity` 场景
- **验证方式**：实例计数对账 16235；FPSController 可漫游；同机位截图 vs 源
- **自动化方式**：AI Agent 自动（Editor 脚本驱动）
- **依赖**：T6-1 + T5-3 + T2-4（全部脚本就绪）
- **工时**：4 h

---

#### T6-3 Winter 场景还原（E17）
- **阶段**：6 / E17
- **实现方向**：
  1. 复用 scene_parser → `winter_instances.json`（8087 条）+ 天气/灯光/相机参数
  2. 地形结构复刻（`IL3DN_Terrain_02`）+ 实例摆放
  3. Weather / ColorManager / 灯光 / 雾 / Volume 按 YAML 源值还原
  4. 脚步声用 Dirt/Grass 组代替（源无雪地音效）
- **产出物**：`PineForest_Winter.unity`
- **验证方式**：实例计数对账 8087；可漫游；同机位截图 vs 源 Winter
- **自动化方式**：AI Agent 自动
- **依赖**：T6-2（Demo 装配流程验证通过）
- **工时**：2 h

---

#### T6-4 同机位对比验证
- **阶段**：6 / E18 前置
- **实现方向**：
  1. 固定机位组截图（Demo + Winter 各 5-10 个关键视角）
  2. Python SSIM + 直方图距离计算
  3. 阈值：SSIM ≥ 0.75 通过
- **产出物**：对比报告（含 SSIM 值 + borderline 清单）
- **验证方式**：报告产出；borderline 案例人眼终审
- **自动化方式**：AI Agent 自动（脚本驱动截图 + Python 计算）
- **依赖**：T6-2 + T6-3
- **工时**：2 h

---

### 阶段 7：调优与验收（对应 E18）

---

#### T7-1 性能调优
- **阶段**：7 / E18
- **实现方向**：
  1. Profiler 找瓶颈（目标 60fps @ 1080p 中端机）
  2. GPU Instancing / LOD / Billboard Distance 调参
  3. `_OneMesh` 变体启用
- **产出物**：调参后的场景文件 + Profiler 报告
- **验证方式**：目标帧率达标；同机位画面对比无显著退化
- **自动化方式**：半自动（AI Agent 调参 + 人审帧率）
- **依赖**：T6-4
- **工时**：3 h

---

#### T7-2 量化验收
- **阶段**：7 / E18
- **实现方向**：
  1. 资产对账：mat 38 / prefab 83 / 实例 16235+8087 / shader 编译 0 error
  2. 全局变量名逐一 diff
  3. README.md（项目结构、运行方式、工具链说明）
- **产出物**：验收报告 + README.md
- **验证方式**：全部指标通过
- **自动化方式**：AI Agent 自动（脚本对账）
- **依赖**：T7-1
- **工时**：2 h

---

### 阶段 8：贯穿自动化工具（与阶段并行开发）

---

#### T8-1 Builder_Screenshots（固定机位截图）
- **阶段**：贯穿 / 阶段 6 起可用
- **实现方向**：`Scripts/Editor/MyNatureBuilder_Screenshots.cs`：固定机位列表 → 每个机位 `ScreenCapture.CaptureScreenshot` 或 `RenderTexture` → 输出 PNG
- **产出物**：Editor 脚本 + 机位配置文件
- **验证方式**：batchmode 运行后 PNG 文件存在且 1080p
- **自动化方式**：AI Agent 自动
- **依赖**：T1-9（需要可编译的项目）
- **工时**：2 h

---

#### T8-2 Builder_Recorder（漫游录屏）
- **阶段**：贯穿 / 阶段 6 起可用
- **实现方向**：`Scripts/Editor/MyNatureBuilder_Recorder.cs`：`RecorderControllerSettings` + `MovieRecorder` + 代码驱动相机路径（AnimationCurve/航点）
- **产出物**：Editor 脚本 + 相机路径配置
- **验证方式**：batchmode 运行后输出 1080p30 mp4
- **自动化方式**：AI Agent 自动
- **依赖**：T0-2（Recorder 包安装）+ T1-9
- **工时**：3 h

---

#### T8-3 SSIM 对比 Python 脚本
- **阶段**：贯穿 / 阶段 6 起可用
- **实现方向**：`tools/compare_screenshots.py`：读源/复刻各一组 PNG → 计算 SSIM（skimage）+ 直方图距离 → 输出 JSON 报告
- **产出物**：Python 脚本
- **验证方式**：对同一图算 SSIM = 1.0；对明显差异图算 SSIM < 0.5
- **自动化方式**：AI Agent 自动
- **依赖**：无（纯 Python）
- **工时**：2 h

---

#### T8-4 video_lint.py
- **阶段**：贯穿 / 视频阶段可用
- **实现方向**：`tools/video_lint.py`：ffprobe 检查 clips 参数 / meta.md 字段完整 / script.md @visual 路径存在 / 块 ID 递增 / 旁白行 ≤50 字
- **产出物**：Python 脚本
- **验证方式**：跑一遍输出 lint 报告，无 ERROR
- **自动化方式**：AI Agent 自动
- **依赖**：ffmpeg 已装
- **工时**：2 h

---

## 3. 视频任务（V01-V18）

> 视频任务与工程任务**并行**。每集视频 = 录屏素材 + HTML 截图 + script.md + meta.md → BUILD.md 构建。
> 视频任务**不阻塞**工程开发，但工程资产是视频素材的前提。

| 编号 | 名称 | 工程前提 | 素材类型 | 工时 |
|---|---|---|---|---|
| V01 | 项目全景与工程骨架 | T0-1, T0-2 | 目录创建、Blender 立方体、Python PNG、终端 | 2 h |
| V02 | 灯光雾 Volume | T0-2 | 灯光调试、Volume 滑杆、雾开关 | 2 h |
| V03 | Sky shader | T1-2 | shader 逐段编写、天空参数变化 | 3 h |
| V04 | Blender 第一棵树 | T4-1 + T3-1 | MCP 建模、FBX 导入、贴图换装 | 3 h |
| V05 | 风系统 + Branch | T1-3 + T2-1 | shader 讲解、风滑杆、有风/无风对比 | 3 h |
| V06 | 批量草花岩 | T4-2 | 配置表、批量生成、检阅 | 3 h |
| V07 | Grass/Leaf/Pine + 雪 | T1-4 + T2-1 | 3 shader 差异、雪量滑杆 0→1 | 4 h |
| V08 | 树族建模 | T4-3 | 参数差异、树林风吹 | 3 h |
| V09 | 卡片辅助 shader | T1-5 + T1-6 | 6 shader 聚焦 trick、地形 splat | 3 h |
| V10 | Python 全量贴图 | T3-2 + T3-3 | 生成过程、替换前后对比 | 3 h |
| V11 | Water shader | T1-7 | 5 pass 功能块、泡沫/深度/Fresnel | 4 h |
| V12 | Fog Renderer Feature | T1-8 | 前后对比、三段调色 | 3 h |
| V13 | FPS + 音频 + ColorManager | T2-2 + T2-3 + T2-4 | 漫游、脚步、季节切换 | 3 h |
| V14 | Editor 批量装配 | T5-1 + T5-2 + T5-3 | 菜单涌现、batchmode 无人值守 | 3 h |
| V15 | 地形 + 精确还原 | T6-0 + T6-1 + T6-2 | 解析脚本、摆放延时、穿林漫游 | 3 h |
| V16 | Demo 装配 | T6-2 | 装配过程、bug 修复、同机位对比 | 3 h |
| V17 | Winter 场景 | T6-3 | 冬季漫游、夏冬对比 | 3 h |
| V18 | 性能调优 + 总结 | T7-1 + T7-2 | Profiler、回顾工作流 | 2 h |

> 视频总工时估算：**~52 h**（不含 BUILD.md 构建时间）。
> 视频与工程**可并行**：V01-V03 在阶段 0-1 时即可开始录；V04 需等 T4-1+T3-1；以此类推。

---

## 4. 关键路径与并行策略

```
关键路径（决定总工期）：
  T1-0 → T1-1 → T1-3 → T1-4 → T1-5 → T1-6 → T1-7 → T1-8 → T1-9
  → T2-1 → T2-2 → T2-3 → T2-4
  → T5-1 → T5-2 → T5-3
  → T6-0 → T6-1 → T6-2 → T6-3 → T6-4
  → T7-1 → T7-2

可并行（不阻塞关键路径）：
  · T3-0~T3-3（贴图）可与 T1-2~T1-9 部分并行
  · T4-0~T4-4（模型）可与 T1-5~T1-9 部分并行
  · T8-1~T8-4（工具）可与阶段 1-2 并行开发
  · V01~V18（视频）与工程全程并行
```

**最早可开工项**（无需等待任何工程前提）：
- T1-0（05 规格）
- T3-0（07 规格）
- T4-0（06 规格）
- T6-0（scene_parser，纯 Python，不依赖 Unity）
- T8-3（SSIM 脚本，纯 Python）
- T8-4（video_lint，纯 Python）

---

## 5. 任务执行顺序建议（第一批）

1. **立刻执行**（0 依赖，今天可做）：
   - T0-1（骨架 + 音频 + gitignore 提交）
   - T0-2（配置 + 四链路验证 + Recorder 包安装）
   - T1-0（05 规格 shader 部分）
   - T3-0（07 贴图规格最小包部分）
   - T4-0（06 模型规格 Pine 部分）
   - T6-0（scene_parser 开发）

2. **第二批**（等 T1-0 完成）：
   - T1-1 ~ T1-4（Include + Sky + Branch + Grass/Leaf/Pine）
   - T3-1（最小贴图包）

3. **第三批**（等 shader 骨架跑通）：
   - T2-1（Wind/Snow）
   - T4-1（Pine_01 建模）
   - T8-1（Builder_Screenshots）

---

*下一步：按本任务表执行。状态追踪见 `09_任务状态_开发看板.md`。*
